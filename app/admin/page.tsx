import {redirect} from 'next/navigation';import Header from '../../components/Header';import AdminManager from '../../components/AdminManager';import PeopleImport from '../../components/PeopleImport';import {isAdmin} from '../../lib/admin';import {readAllocations,readCampaigns,readRegistrations,readSpeakerInvites} from '../../lib/registration';import {readRequests} from '../../lib/attendance';export const dynamic='force-dynamic';
export default async function Page(){if(!await isAdmin())redirect('/admin/login');const [requests,regs,campaigns,allocations,speakers]=await Promise.all([readRequests(),readRegistrations(),readCampaigns(),readAllocations(),readSpeakerInvites()]);const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';return <main className="admin-page"><section className="admin-hero"><Header/><div className="shell"><p className="eyebrow">Banking CEE Admin</p><h1>Invitation & Registration Control.</h1><p>Create controlled campaigns, organisation allocations and private speaker invitations.</p><div className="admin-stats"><div><strong>{requests.length}</strong><span>Attendance requests</span></div><div><strong>{regs.length}</strong><span>Registrations</span></div><div><strong>{campaigns.length}</strong><span>Campaigns</span></div><div><strong>{allocations.length}</strong><span>Allocations</span></div><div><strong>{speakers.length}</strong><span>Speaker invites</span></div></div></div></section><section className="section-white"><div className="shell"><AdminManager baseUrl={base}/><div style={{marginTop:'28px'}}><PeopleImport/></div><div className="admin-list"><h2>Current invitations</h2><h3>Campaigns</h3>{campaigns.map(c=><div className="admin-row" key={c.code}><span><strong>{c.name}</strong><small>{c.used}/{c.capacity} used · {c.active?'Active':'Closed'}</small></span><code>{base}/register/campaign/{c.code}</code></div>)}<h3>Organisation allocations</h3>{allocations.length?allocations.map(a=><div className="admin-row" key={a.code}><span><strong>{a.organisation}</strong><small>{a.used}/{a.capacity} used · {a.participantType}</small></span><code>{base}/register/organisation/{a.code}</code></div>):<p className="muted">No allocations created yet.</p>}<h3>Speaker invitations</h3>{speakers.length?speakers.map(s=><div className="admin-row" key={s.code}><span><strong>{s.personName||'Open nomination'}</strong><small>{s.organisation||'No organisation'} · {s.used?'Used':'Available'}</small></span><code>{base}/register/speaker/{s.code}</code></div>):<p className="muted">No speaker invitations created yet.</p>}</div></div></section></main>}
