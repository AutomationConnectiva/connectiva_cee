import {NextResponse} from 'next/server';import {isAdmin} from '../../../../lib/admin';import {readAllocations,secureCode,writeAllocations} from '../../../../lib/registration';
export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.organisation||!b.event||!b.capacity)return NextResponse.json({error:'Missing required fields'},{status:400});const all=await readAllocations();const item={code:secureCode('org'),name:b.name||`${b.organisation} allocation`,event:b.event,organisation:b.organisation,participantType:b.participantType||'sponsor',capacity:Number(b.capacity),used:0,active:true,expiresAt:b.expiresAt||undefined};all.push(item);await writeAllocations(all);return NextResponse.json({ok:true,item})}
