import {NextResponse} from 'next/server';import {isAdmin} from '../../../../lib/admin';import {readCampaigns,secureCode,writeCampaigns} from '../../../../lib/registration';
export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.name||!b.event||!b.capacity)return NextResponse.json({error:'Missing required fields'},{status:400});const all=await readCampaigns();const item={code:secureCode('camp'),name:b.name,event:b.event,participantType:b.participantType||'delegate',capacity:Number(b.capacity),used:0,approvalRequired:!!b.approvalRequired,active:true,expiresAt:b.expiresAt||undefined};all.push(item);await writeCampaigns(all);return NextResponse.json({ok:true,item})}
