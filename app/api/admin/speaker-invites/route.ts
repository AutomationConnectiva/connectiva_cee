import {NextResponse} from 'next/server';import {isAdmin} from '../../../../lib/admin';import {readSpeakerInvites,secureCode,writeSpeakerInvites} from '../../../../lib/registration';
export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.event)return NextResponse.json({error:'Event required'},{status:400});const all=await readSpeakerInvites();const item={code:secureCode('spk'),event:b.event,organisation:b.organisation||undefined,personName:b.personName||undefined,email:b.email||undefined,used:false,active:true,expiresAt:b.expiresAt||undefined};all.push(item);await writeSpeakerInvites(all);return NextResponse.json({ok:true,item})}
