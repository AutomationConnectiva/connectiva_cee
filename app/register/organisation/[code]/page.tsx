import Header from '../../../../components/Header';import RegistrationForm from '../../../../components/RegistrationForm';import {eventLabel} from '../../../../lib/attendance';import {readAllocations} from '../../../../lib/registration';export const dynamic='force-dynamic';
export default async function Page({params}:{params:Promise<{code:string}>}){const {code}=await params;const all=await readAllocations();const a=all.find(x=>x.code===code&&x.active);if(!a||a.used>=a.capacity)return <main><section className="contact-hero"><Header/><div className="shell contact-content"><p className="eyebrow">Organisation Registration</p><h1>Allocation Unavailable.</h1><p>This registration allocation is closed, invalid or has reached its limit.</p></div></section></main>;return <main><section className={`registration-hero ${a.event==='summit-2027'?'registration-summit':''}`}><Header/><div className="shell registration-hero-copy"><p className="eyebrow">Organisation Registration</p><h1>{a.organisation}</h1><p>{a.capacity-a.used} of {a.capacity} registrations remaining · {eventLabel(a.event)}</p></div></section><section className="section-white"><div className="shell registration-shell"><RegistrationForm mode="allocation" tokenOrCode={code} event={a.event} initial={{organisation:a.organisation}} campaignName={a.name}/></div></section></main>}
