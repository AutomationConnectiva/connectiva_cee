'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function AdminLogin(){const [password,setPassword]=useState(''),[error,setError]=useState('');const r=useRouter();async function submit(e:FormEvent){e.preventDefault();const res=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});if(!res.ok){setError('Invalid password');return}r.push('/admin');r.refresh()}return <form onSubmit={submit} className="admin-login-card"><h2>Admin Access</h2><label><span>Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="form-error">{error}</p>}<button className="btn primary" type="submit">SIGN IN →</button></form>}
